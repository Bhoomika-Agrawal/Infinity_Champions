class Solution {
    public int eraseOverlapIntervals(int[][] intervals) {
        Arrays.sort(intervals,(a,b)->Integer.compare(a[1],b[1]));
        int o=1;
        int first=intervals[0][1];
        for(int i=0;i<intervals.length;i++){
            if(intervals[i][0]>=first){
                o++;
                first=intervals[i][1];
            }
        }
        return intervals.length-o;
    }
}
